# =============================================================
# STIRLING PDF — ENVIRONMENT VARIABLES
# =============================================================
# Copy this file to .env and fill in your values
# NEVER commit the .env file — only .env.example
# Shared variables (ROOT_DOMAIN, DOCKER_DATA_PATH, TZ)
# come from root .env
#
# Stirling PDF is fully stateless. All configuration is
# handled via environment variables in docker-compose.yml.
# No secrets or credentials required.
# =============================================================
