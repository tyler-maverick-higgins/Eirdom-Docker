# =============================================================
# Eirdom — Home Assistant — service-specific environment
# =============================================================
# No service-specific values required.
# Root .env provides: TZ, DOCKER_DATA_PATH, ROOT_DOMAIN
#
# This file exists to satisfy the per-service env_file pattern
# (ADR-025). Leave empty unless a future integration needs a
# secret scoped to this service only.
