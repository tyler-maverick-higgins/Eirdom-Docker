# =============================================================
# UPTIME KUMA — ENVIRONMENT VARIABLES
# =============================================================
# Copy this file to .env and fill in your values
# NEVER commit the .env file — only .env.example
# Shared variables (ROOT_DOMAIN, DOCKER_DATA_PATH, TZ)
# come from root .env
#
# Uptime Kuma has no required environment variables beyond
# what the root .env provides. This file is a placeholder
# for any future configuration overrides.
#
# Default admin credentials are set on first login at:
# https://status.eirdom.homes
# Set a strong password and save to password manager.
# =============================================================
